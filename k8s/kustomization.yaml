apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: infinance
resources:
  - namespace.yaml
  - configmap.yaml
  # Create infinance-secrets out of band; see README deployment instructions.
  - mysql.yaml
  - backend.yaml
  - frontend.yaml
  - ingress.yaml

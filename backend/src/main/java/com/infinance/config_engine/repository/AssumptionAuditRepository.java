package com.infinance.config_engine.repository;

import com.infinance.config_engine.entity.AssumptionAuditEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AssumptionAuditRepository extends JpaRepository<AssumptionAuditEntity, Long> {}
